name: Build and push BlueOS extension

on:
  push:
    branches: [main]
    tags: ['*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build and push (Pi4/Pi5 arm64 + amd64 — ESPHome upstream has no arm/v7 image)
        uses: BlueOS-community/Deploy-BlueOS-Extension@v1.2.0
        with:
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          build-platforms: linux/arm64/v8,linux/amd64
          image-name: esphome
          image-prefix: blueos-site-
          author: Tony White
          author-email: tony@bluerobotics.com
          maintainer: Tony White
          maintainer-email: tony@bluerobotics.com
